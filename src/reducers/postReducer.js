//import { GET_USERS, NEW_POST } from '../actions/types';

const initialState = {
    items: [],
    item: {}
}

export const postReducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;

    }

}